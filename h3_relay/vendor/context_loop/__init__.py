"""Vendored MiniMax H3 Context Loop runtime.

This package intentionally does not register the upstream public node map.
H3 Relay imports the implementation classes it needs and exposes namespaced
node ids from its own entrypoint.
"""
